import { Button } from "@mui/material";

const ProfileSetup = () => {
  return (
    <div>
      Profile Setup
      <Button href="/dashboard">Dashboard</Button>
    </div>
  );
};

export default ProfileSetup;
